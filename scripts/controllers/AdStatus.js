app.controller('AdStatus', ['$scope', '$rootScope', 'adStatus', 'filter', function($scope, $rootScope, adStatus, filter){

    //categoriesData.getCategories().$promise.then(function(data) {
    //    $scope.statuses = data;
    //});

    //$scope.statusFilter = function statusFilter(category) {
    //    filter.filterByStatus(category);
    //    $rootScope.$broadcast('statusFilter', category);
    //}
}]);